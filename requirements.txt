# IndicParlerTTS F0-anchored narrator pipeline
# Install order matters — parler_tts must come from source

# ── IndicParlerTTS (install from source) ─────────────────────────────────────
# pip install git+https://github.com/huggingface/parler-tts.git

# ── Core ML ───────────────────────────────────────────────────────────────────
torch>=2.1.0
transformers>=4.40.0
accelerate>=0.27.0

# ── Speaker embedding ─────────────────────────────────────────────────────────
speechbrain>=1.0.0

# ── F0 extraction (Praat via Python bindings — CPU, fast) ─────────────────────
praat-parselmouth>=0.4.3

# ── Audio I/O ─────────────────────────────────────────────────────────────────
soundfile>=0.12.1

# ── Resampling (optional but recommended for ECAPA 16kHz alignment) ───────────
librosa>=0.10.0

# ── Core numerics ─────────────────────────────────────────────────────────────
numpy>=1.24.0
